#include "main.h"

/**
 * _isalpha - entry point, checks for alphabetic character
 *
 * @c: integer used
 * Return: 1 or 0.
 */

int _isalpha(int c);
{
	if ((c >= 'a' && c <= 'z') || if (c >= 'A' && c <= 'A'))
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
